# CLAUDE.md

All guidance for this repo lives in [AGENTS.md](./AGENTS.md). Read it before making changes.

It is the single source of truth: what this project is, the command table, the
non-negotiable pre-commit checks, the type-hygiene rules the ESLint config enforces, where
things live, and the registry/ATS conventions.

Do not duplicate guidance here. If a rule needs to change, change it in AGENTS.md so both
humans and every agent tool read the same instructions.
