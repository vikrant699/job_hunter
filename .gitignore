# dependencies + build
node_modules/
dist/

# secrets + runtime data
.env
.env.local
data/

# user profile (your personal resume / deal-breakers / locations)
config/profile.ts
config/resume.pdf
config/resume.txt

# logs
*.log

# editor / IDE
.claude/
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
Desktop.ini

# yaml/json backup files
*.bak
*.tmp-*

# local planning / working notes (never publish)
docs/

# recruiter contact dump (personal data, local-only; live Raw Data tab is the source)
config/recruiters-raw.csv

# named multi-profile dirs (config/profiles/<name>/profile.ts + resume.*) — local only
config/profiles/*
!config/profiles/.gitkeep
